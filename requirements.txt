# Verified 2026-09-10 by actually installing into a clean venv (see SETUP_NOTES.md).
# rocketride requires Python 3.11+ (uses typing.NotRequired) — use Python 3.12.
cognee>=1.5.4
hydradb-sdk>=2.1.4
hotdata-framework>=0.14.0
rocketride>=1.3.0
python-dotenv>=1.2.3
